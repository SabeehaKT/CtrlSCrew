# Copy this file to .env and fill in the values:
#   copy .env.example .env   (Windows)
#   cp .env.example .env     (Mac/Linux)

# Required: Generate a secure key with: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=your-secret-key-here-change-in-production

# Required: SQLite database (default). Use full path if running from another directory.
DATABASE_URL=sqlite:///./employee_portal.db

# Optional (defaults shown)
# ALGORITHM=HS256
# ACCESS_TOKEN_EXPIRE_MINUTES=30
# ALLOWED_ORIGINS=http://localhost:3000
